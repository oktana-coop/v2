// Default folder (relative to the project root) into which new assets are inserted.
// Existing assets may live anywhere in the workdir — this is just the default.
export const DEFAULT_ASSETS_DIR_NAME = 'assets';
