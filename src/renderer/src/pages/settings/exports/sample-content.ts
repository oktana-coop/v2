export const sampleContent = `
<h1>Template Preview</h1>

<p>This document demonstrates how your content will appear when exported using the selected template. Adjust typography, spacing, and color to define the overall presentation.</p>

<p>This paragraph contains <em>emphasis</em>, <strong>strong text</strong>, a <a href="#">link</a>, and <code>inline code</code> to illustrate how inline styles appear within body text.</p>

<p>Consistent formatting improves readability and helps establish visual hierarchy across sections.</p>

<h2>Configuration</h2>

<h3>Structure and Hierarchy</h3>

<p>Headings define structure. Paragraphs provide detail. Lists organize related information. Code blocks separate technical content from narrative text.</p>

<p>Adjust font size, weight, spacing, and alignment to see how these elements interact.</p>

<blockquote>
<p>This example blockquote demonstrates how supporting information can be visually separated from the main content. Modify indentation, border styling, or spacing to control emphasis and contrast.</p>
</blockquote>

<h2>Lists</h2>

<h3>Unordered List</h3>

<ul>
<li>Primary feature description</li>
<li>Secondary item with <em>emphasis</em></li>
<li>Supporting detail with <strong>strong text</strong></li>
<li>Technical reference using <code>inline code</code></li>
<li>Nested structure
  <ul>
    <li>Nested item one</li>
    <li>Nested item two</li>
  </ul>
</li>
</ul>

<h3>Ordered List</h3>

<ol>
<li>Define document styles</li>
<li>Review changes in the preview</li>
<li>Refine spacing and hierarchy</li>
</ol>

<h4>Code Example</h4>

<pre><code>function greet(name) {
  return "Hello, " + name + "!";
}

console.log(greet("World"));</code></pre>

<h5>Additional Notes</h5>

<p>Fine-tune heading levels to create a clear visual hierarchy throughout your document.</p>

<h6>Footer Reference</h6>

<p>This is the smallest heading level, useful for minor annotations or references.</p>
`;
