import { EmptyDocument } from '../../../../components/document-views/EmptyDocument';

export const HistoryNoProject = () => {
  return <EmptyDocument message="Open a project to view its history." />;
};
