# Production build config, loaded automatically by `pnpm run build` (electron-vite, production mode).
# These VITE_ values are baked into the bundle at build time and are public.
# Real env vars (e.g. from CI) take precedence over this file, so this is the
# default fallback that lets a clean source build produce a correct app.
VITE_PRIMARY_RICH_TEXT_REPRESENTATION="MARKDOWN"
VITE_GITHUB_APP_CLIENT_ID="Iv23lin5DIAsGGXyb2XO"
